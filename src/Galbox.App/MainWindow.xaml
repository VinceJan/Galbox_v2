<Window
    x:Class="Galbox.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop/>
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <Grid
            Grid.Row="0"
            x:Name="AppTitleBar"
            Background="Transparent">
            <TextBlock
                x:Name="AppTitleTextBlock"
                Text="Galbox"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Margin="16,0,0,0"
                FontWeight="SemiBold"/>
        </Grid>

        <!-- Navigation View -->
        <NavigationView
            Grid.Row="1"
            x:Name="NavigationView"
            IsBackButtonVisible="Visible"
            IsSettingsVisible="True"
            PaneDisplayMode="Left"
            ItemInvoked="NavigationView_ItemInvoked"
            BackRequested="NavigationView_BackRequested">

            <NavigationView.MenuItems>
                <NavigationViewItem
                    Content="主页"
                    Tag="Home">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Home"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem
                    Content="游戏库"
                    Tag="Library">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Library"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem
                    Content="存档管理"
                    Tag="SaveManager">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Save"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem
                    Content="补丁中心"
                    Tag="PatchCenter">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Download"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <!-- W2: navigation entries for two modules that were implemented and registered in
                     DI but had no page/nav key/menu item at all (they were unreachable). -->
                <NavigationViewItem
                    Content="刮削进度"
                    Tag="ScrapingProgress">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Sync"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem
                    Content="错误报告"
                    Tag="ErrorReport">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Repair"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <Frame x:Name="ContentFrame"/>
        </NavigationView>
    </Grid>
</Window>