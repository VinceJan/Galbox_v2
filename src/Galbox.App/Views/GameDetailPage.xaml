<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Galbox.App.Views.GameDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Back Button -->
        <Button Grid.Row="0"
                Icon="Back"
                Click="Back_Click"
                Margin="0,0,0,16"/>

        <!-- Game Info Section -->
        <Grid Grid.Row="1" ColumnSpacing="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>

            <!-- Cover -->
            <Border Grid.Column="0"
                    Width="180"
                    Height="260"
                    Background="{ThemeResource SystemControlBackgroundAccentBrush}"
                    CornerRadius="8">
                <TextBlock Text="封面" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White"/>
            </Border>

            <!-- Game Details -->
            <StackPanel Grid.Column="1" Spacing="8">
                <TextBlock x:Name="GameTitleText" Text="游戏名称" Style="{StaticResource TitleTextBlockStyle}"/>
                <TextBlock x:Name="GameOriginalTitleText" Text="原名" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>

                <!-- Launch Button -->
                <Button Content="启动游戏"
                        Style="{StaticResource AccentButtonStyle}"
                        Margin="0,16,0,0">
                    <Button.Flyout>
                        <MenuFlyout>
                            <MenuFlyoutItem Text="主程序" Click="LaunchMain_Click"/>
                            <MenuFlyoutItem Text="日文原版" Click="LaunchAlt_Click"/>
                            <MenuFlyoutSeparator/>
                            <MenuFlyoutItem Text="设置默认启动程序"/>
                        </MenuFlyout>
                    </Button.Flyout>
                </Button>

                <!-- Description -->
                <TextBlock Text="游戏简介" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,24,0,8"/>
                <TextBlock x:Name="GameDescriptionText"
                           Text="游戏简介内容..."
                           TextWrapping="Wrap"
                           MaxHeight="150"/>

                <!-- Tags -->
                <TextBlock Text="标签" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,16,0,8"/>
                <StackPanel Orientation="Horizontal" Spacing="4">
                    <Button Content="恋爱"/>
                    <Button Content="校园"/>
                    <Button Content="治愈"/>
                </StackPanel>

                <!-- Characters -->
                <TextBlock Text="角色" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,16,0,8"/>
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Border Width="60" Height="60" Background="{ThemeResource SystemControlBackgroundAccentBrush}" CornerRadius="4">
                        <TextBlock Text="角色1" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </StackPanel>
            </StackPanel>

            <!-- Stats Panel -->
            <Border Grid.Column="2"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="8"
                    Padding="16">
                <StackPanel Spacing="16">
                    <TextBlock Text="游玩统计" Style="{StaticResource SubtitleTextBlockStyle}"/>

                    <StackPanel Spacing="4">
                        <TextBlock Text="最近游玩" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                        <TextBlock x:Name="LastPlayedText" Text="2026-04-10 15:30"/>
                    </StackPanel>

                    <StackPanel Spacing="4">
                        <TextBlock Text="游玩时长" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                        <TextBlock x:Name="PlayTimeText" Text="15小时"/>
                    </StackPanel>

                    <StackPanel Spacing="4">
                        <TextBlock Text="游玩次数" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                        <TextBlock x:Name="PlayCountText" Text="8次"/>
                    </StackPanel>

                    <StackPanel Spacing="4">
                        <TextBlock Text="存档备份" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                        <Button Content="查看存档" Click="ViewSaves_Click"/>
                        <Button Content="快速备份" Click="QuickBackup_Click"/>
                    </StackPanel>

                    <StackPanel Spacing="4">
                        <TextBlock Text="说明文档" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                        <Button Content="README.txt" Click="OpenReadme_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Media Section -->
        <StackPanel Grid.Row="2" Spacing="16" Margin="0,24,0,0">
            <TextBlock Text="音乐播放器" Style="{StaticResource SubtitleTextBlockStyle}"/>
            <TextBlock Text="视频展示" Style="{StaticResource SubtitleTextBlockStyle}"/>
            <TextBlock Text="截图收藏" Style="{StaticResource SubtitleTextBlockStyle}"/>
        </StackPanel>
    </Grid>

</Page>