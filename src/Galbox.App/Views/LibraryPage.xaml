<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Galbox.App.Views.LibraryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header with Search and Controls -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8">
            <TextBlock Text="游戏库" Style="{StaticResource TitleTextBlockStyle}"/>
            <TextBox x:Name="SearchTextBox"
                     PlaceholderText="搜索游戏..."
                     Width="300"
                     QuerySubmitted="SearchTextBox_QuerySubmitted"/>
            <Button Content="筛选" Icon="Filter"/>
            <Button Content="添加游戏" Style="{StaticResource AccentButtonStyle}" Click="AddGame_Click"/>
            <Button x:Name="ToggleViewButton" Icon="List" Click="ToggleView_Click"/>
        </StackPanel>

        <!-- Status Filter Tabs -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8" Margin="0,16,0,0">
            <Button Content="全部" Click="Filter_Click" Tag="All"/>
            <Button Content="正在游玩" Click="Filter_Click" Tag="Playing"/>
            <Button Content="已完成" Click="Filter_Click" Tag="Completed"/>
            <Button Content="待玩" Click="Filter_Click" Tag="ToPlay"/>
            <Button Content="暂停中" Click="Filter_Click" Tag="Paused"/>
        </StackPanel>

        <!-- Games Grid/List -->
        <ScrollViewer Grid.Row="2" Margin="0,16,0,0">
            <GridView x:Name="GamesGridView"
                      SelectionMode="None"
                      IsItemClickEnabled="True"
                      ItemClick="Game_Click">
                <GridView.ItemTemplate>
                    <DataTemplate>
                        <Grid Width="200" Height="280" Margin="8">
                            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8">
                                <Grid>
                                    <!-- Cover Placeholder -->
                                    <Border Height="180" CornerRadius="8"
                                            Background="{ThemeResource SystemControlBackgroundAccentBrush}">
                                        <TextBlock Text="封面"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"
                                                   Foreground="White"/>
                                    </Border>

                                    <!-- Game Info -->
                                    <StackPanel VerticalAlignment="Bottom" Padding="12">
                                        <TextBlock Text="{Binding NameCn}"
                                                   TextWrapping="Wrap"
                                                   MaxLines="2"
                                                   Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                        <TextBlock Text="{Binding StatusText}"
                                                   Style="{StaticResource CaptionTextBlockStyle}"
                                                   Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </Grid>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>
        </ScrollViewer>
    </Grid>

</Page>