<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Galbox.App.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="Galbox 主页"
                   Style="{StaticResource TitleTextBlockStyle}"
                   Margin="0,0,0,16"/>

        <!-- Quick Launch Card -->
        <Grid Grid.Row="1"
              Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
              CornerRadius="8"
              Padding="16"
              Height="120"
              ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Game Cover Placeholder -->
            <Border Grid.Column="0"
                    Background="{ThemeResource SystemControlBackgroundAccentBrush}"
                    CornerRadius="4">
                <TextBlock Text="封面"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}"/>
            </Border>

            <!-- Game Info -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock Text="最近游玩的游戏" Style="{StaticResource SubtitleTextBlockStyle}"/>
                <TextBlock Text="最近游玩: 刚刚" Style="{StaticResource BodyTextBlockStyle}" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                <TextBlock Text="游玩时长: 15小时" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
            </StackPanel>

            <!-- Launch Button -->
            <Button Grid.Column="2"
                    Content="立即启动"
                    Style="{StaticResource AccentButtonStyle}"
                    VerticalAlignment="Center"
                    Click="QuickLaunch_Click"/>
        </Grid>

        <!-- Recent Games Section -->
        <TextBlock Grid.Row="2"
                   Text="最近游戏"
                   Style="{StaticResource SubtitleTextBlockStyle}"
                   Margin="0,24,0,8"/>

        <!-- Recent Games Grid -->
        <ScrollViewer Grid.Row="3">
            <GridView x:Name="RecentGamesGridView"
                      SelectionMode="None"
                      IsItemClickEnabled="True"
                      ItemClick="RecentGame_Click">
                <GridView.ItemTemplate>
                    <DataTemplate>
                        <Grid Width="180" Height="240" Margin="8">
                            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8">
                                <StackPanel VerticalAlignment="Bottom" Padding="12">
                                    <TextBlock Text="{Binding NameCn}" TextWrapping="Wrap" MaxLines="2"/>
                                    <TextBlock Text="{Binding LastPlayedText}"
                                               Style="{StaticResource CaptionTextBlockStyle}"
                                               Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>
        </ScrollViewer>
    </Grid>

</Page>