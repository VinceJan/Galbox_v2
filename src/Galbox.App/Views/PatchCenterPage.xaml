<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Galbox.App.Views.PatchCenterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="补丁中心" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,16"/>

        <!-- Search -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8" Margin="0,0,0,16">
            <TextBox x:Name="SearchTextBox"
                     PlaceholderText="搜索补丁..."
                     Width="300"
                     QuerySubmitted="SearchTextBox_QuerySubmitted"/>
            <ComboBox PlaceholderText="补丁类型"
                      Width="150">
                <ComboBoxItem Content="全部"/>
                <ComboBoxItem Content="汉化补丁"/>
                <ComboBoxItem Content="修复补丁"/>
                <ComboBoxItem Content="年龄限制解除"/>
            </ComboBox>
        </StackPanel>

        <!-- Patch List -->
        <ScrollViewer Grid.Row="2">
            <StackPanel Spacing="8">
                <!-- Patch Card Example -->
                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        CornerRadius="8"
                        Padding="16">
                    <Grid ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Game Cover Placeholder -->
                        <Border Grid.Column="0"
                                Height="100"
                                Background="{ThemeResource SystemControlBackgroundAccentBrush}"
                                CornerRadius="4">
                            <TextBlock Text="封面"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Foreground="White"/>
                        </Border>

                        <!-- Patch Info -->
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="同居恋人汉化补丁 v1.2" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                            <TextBlock Text="类型: 汉化补丁 | 大小: 15MB | 更新: 2025-01-15"
                                       Style="{StaticResource CaptionTextBlockStyle}"
                                       Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                            <TextBlock Text="完整汉化，支持最新版本"
                                       Style="{StaticResource CaptionTextBlockStyle}"
                                       Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                       MaxLines="2"
                                       TextWrapping="Wrap"/>
                        </StackPanel>

                        <!-- Actions -->
                        <StackPanel Grid.Column="2" VerticalAlignment="Center" Spacing="8">
                            <Button Content="下载" Style="{StaticResource AccentButtonStyle}"/>
                            <Button Content="详情"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- More patch cards... -->
                <TextBlock Text="更多补丁将在此处显示..."
                           HorizontalAlignment="Center"
                           Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>

</Page>