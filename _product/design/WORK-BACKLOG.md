# Galbox 主任务清单（Work Backlog）

> 编制时间：2026-09-11　｜　维护者：主对话
> 来源：四份调研报告 + 实测发现。**每一项都有证据来源，不是拍脑袋排的。**
> 状态标记：✅ 已完成 ｜ 🔄 进行中 ｜ ⬜ 待办 ｜ ⛔ 被产品决策阻塞

---

## 0. 项目当前的真实状态（一句话）

**代码能编译、能启动（修复后），但绝大部分用户可见功能处于"接线断开"状态**。底层实现比外表好得多——问题集中在四类：**接线、入口、UI 绑定、DI 生存期**。

**唯一客观的度量**：无界面验收程序（`tests\Galbox.Acceptance`），8 项检查。
**首轮基线（2026-09-11）**：A0/A2/A3/A4/A7 PASS，**A1/A5/A6 FAIL**。

---

## 1. ✅ 已完成

| # | 事项 | 证据 |
|---|---|---|
| 1 | **阻断级修复**：应用启动弹不出窗口（三个 P/Invoke 声明在 user32.dll，实际由 comctl32.dll 导出） | 二进制扫描两 DLL + 实机 A/B：修复前 `MainWindowHandle=0`，修复后 `=2362014` |
| 2 | **无界面验收程序**（8 项检查，UI-free 复刻 DI 容器） | 已跑通，输出基线报告 |
| 3 | **四份调研报告** | `current-state-audit.md`(108KB) / `scraping-diagnosis.md`(35KB) / `renpy-save-analysis.md`(58KB) / `patch-source-research.md`(55KB) |
| 4 | **用户数据安全网** | 迁移前 DB 快照（26 列、无 VndbId）+ 公开仓库隐私隔离（私人文档已排除） |
| 5 | **产品知识包入库** | `_product\` 下 6 份文档已提交 |

---

## 2. 🔄 进行中（四条并行工作线，各自独立 worktree）

| 工作线 | 目录 / 分支 | 目标 | 验收判据 |
|---|---|---|---|
| **数据层** | `Galbox_data` / `feat/save-node-model` | 存档节点模型、游戏状态字段、**数据库迁移机制** | 用"老库快照"验证：迁移后数据一条不少、新列建出、幂等 |
| **刮削修复** | 主工作区 | 修 D1~D19 缺陷 + 入口 + VndbId | **A5/A6 从 FAIL 转 PASS** + `ScrapingCache\` 出现 `search_*.json` |
| **存档解析器** | `Galbox_parser` / `feat/renpy-parser` | C# 实现 Ren'Py 存档解析（含 RPA 解包、零执行 pickle 扫描） | label 命中 **22/22**、CG **6/27=22.2%**、回滚前后哈希全等 |
| **补丁安装器** | `Galbox_patch` / `feat/patch-installer` | 沙箱解压、覆盖预览、只备份被覆盖文件、manifest、回滚 | 回滚后目录**逐字节一致**、Zip Slip 被拒、Shift-JIS 文件名正确 |

---

## 3. ⬜ 待办（按依赖顺序，不是按重要性）

### 3.1 接线类（"已经写好但没有门"，投入产出比最高）

| # | 事项 | 证据 / 位置 |
|---|---|---|
| W1 | **DI 生存期修复** —— 全应用共用一个永不释放的 `DbContext`，是"随机红条"的根因 | `App.xaml.cs:56` 默认 Scoped + 从根容器解析；应改 `AddDbContextFactory` 或全程 `CreateScope()`；开发期开 `ValidateScopes` |
| W2 | 补 `ErrorReportPage.xaml` + `ScrapingProgressPage.xaml` + 两个导航 key + 菜单项 | 两个 ViewModel 已在 DI 注册但**全工程零 XAML 引用** |
| W3 | 给 `PatchCenterPage` 补 `DataContext = ViewModel;` | 其他 5 页都有，唯独它漏了 → 补丁列表**永久 Collapsed** |
| W4 | 启动进程监控：`App.xaml.cs` 后补 `await processMonitor.StartAsync()` + 为每个游戏 `RegisterGame()` | 实现已就绪（1851 行真轮询），**只缺调用** → 一次性点亮老板键/截图/真实运行检测 |
| W5 | 修事件订阅守卫：`ScrapingProgressViewModel.cs:27` `_eventsSubscribed = true` → `false` | 反向守卫导致进度**永不更新**；另 `:268` 漏 `$` |
| W6 | 修 `SettingsPage.xaml:917` 未注册的转换器 | 设置页最可能的崩溃点 |
| W7 | 修 `RemoveWindowSubclass` 的同一个 DLL 错误 | 已随阻断级修复一并处理，需确认关窗不崩 |

### 3.2 诚实性类（"在说谎、在造假"的功能）

| # | 事项 | 证据 |
|---|---|---|
| W8 | **删除假补丁数据生成**（`stub_trans_*` 会真写进用户库） | `PatchCenterViewModel.cs:269-274`；实测用户库当前 0 条，未造成污染 |
| W9 | 详情页"创建备份/恢复"改为调真实服务，或移除按钮 | `GameDetailViewModel.cs:664-676` 只写 DB 行不复制文件；`:706` 直接显示"尚未实现" |
| W10 | 死按钮处理：`GameDetailPage.xaml:471`、`PatchCenterPage.xaml:462/477/505` | 绑定走 `ElementName=RootGrid`，`Command` 恒为 null |
| W11 | **26 个装饰性设置项**：要么接线，要么从 UI 移除 | 37 项里只有 3 项真正被业务读取；`Theme`/`Language` 全仓无应用代码 |
| W12 | 纠正测试的"假绿灯" | `tests\Galbox.Tests` 无任何 ProjectReference、每步 `return true` 零断言，报告却宣称 8/8 通过 |

### 3.3 产品能力类（用户能直接感知的缺失）

| # | 事项 | 证据 |
|---|---|---|
| W13 | **删除游戏**功能 | 全仓 grep 只有"删除扫描目录设置"，**加错/扫错后永远无法移除** |
| W14 | **游戏文件夹丢失检测** | 用户库里唯一那条记录 `SabbatOfTheWitch` 指向的文件夹**已被删除**，界面上仍会显示 |
| W15 | **图片下载链路** | 刮削写 `CoverImageUrl`，但**全工程无任何下载代码**；UI 绑 `CoverImagePath` → 5 个页面封面永远空白 |
| W16 | 窗口标题 | 只用 TextBlock 自绘，没设 `Window.Title` → 任务栏显示 "WinUI Desktop" |
| W17 | 把扫描/添加游戏抽成可测试服务 | 逻辑在 `LibraryViewModel`（无法脱离 UI 测试）；**两份不一致实现**（首页版不识别引擎）；扫描只下探一层 |

### 3.4 数据安全类（可能导致真实损失）

| # | 事项 | 证据 |
|---|---|---|
| W18 | 快速切换补 `currentBackup == null` 短路 | `SaveManagementService.cs:804-810` 缺检查 → 检测失败仍覆盖旧档并**报成功** |
| W19 | 恢复失败分支实现真回滚 | `:519/:534` 的 "Attempting rollback" **只有日志没有代码** |
| W20 | 禁止把 `InstallPath` 当存档目录 | `EngineSaveDetector.cs:645-648` 配合"全删再拷回" → **可能永久损坏游戏安装** |
| W21 | 完整性校验去掉 20%/10% 容差 | `:563-606` 只比文件数且带容差 |

### 3.5 产品灵魂类（差异化所在）

| # | 事项 | 依赖 |
|---|---|---|
| W22 | **存档节点 UI**：时间线视图 / 快照 / 分支分组 | 依赖数据层 + 解析器 |
| W23 | **CG 解锁率展示**（6/27=22.2%，并列出还差哪几张） | 依赖数据层 + 解析器 |
| W24 | 剧情进度展示（已解锁场景数 / 总数，**不要用行号百分比**） | 同上 |
| W25 | **补丁中心**：NextMoe API + BYOK + 跳转下载 + 下载文件夹自动接管 + 状态台账 | 依赖数据层（VndbId）+ 安装器引擎；**受产品决策阻塞** ⛔ |
| W26 | 流程图追踪（预留功能） | 需要自建服务端 → **受产品决策阻塞** ⛔ |
| W27 | 社区成就（存档推断 + 内存监控两条路径） | 同上 ⛔ |

### 3.6 收尾类

| # | 事项 |
|---|---|
| W28 | 打包成**免安装绿色版**（双击即跑，不依赖 .NET 运行时、不需管理员权限） |
| W29 | 全面回归：重跑验收程序，A1/A5/A6 应全部转 PASS |
| W30 | 文档补齐（README / 用户手册 / 开发者指南）+ 推送到 GitHub |
| W31 | 合并四条分支到主线（注意：数据模型有两处并行改动需要协调） |

---

## 4. ⛔ 被产品决策阻塞的项（已记录在 `OPEN-QUESTIONS.md`）

| 决策 | 我的推荐 | 影响 |
|---|---|---|
| 补丁中心密钥模式 | **BYOK**（用户自铸密钥，软件不打包） | 决定 W25 怎么落地 |
| "一键下载"承诺怎么改 | **跳浏览器 + 监听下载文件夹自动接管** | 决定 W25 的交互形态 |
| 玩法状态字段口径 | 恢复 PRD 的 5 值持久化 + 自动推导作默认值 | 数据层已按此实施 |
| 18+ / 破解补丁是否纳入 | 默认不请求，放高级设置 + 年龄确认 | 合规风险 |

---

## 5. 已知的集成风险（合并时要注意）

1. **两套 Schema 管理并存**：刮削代理在 `App.xaml.cs` 加了手工 `ALTER TABLE` 补列循环；数据层代理在建正式 EF 迁移。**合并时后者应取代前者**，否则会打架。
2. **`VndbId` 有两处定义**：已在实体与 DbContext 中定义（`[MaxLength(20)]`），数据层生成迁移时**不要重复定义语义不同的同名字段**。
3. **线上用户库已被手工改过**（27 列，含 VndbId）。**迁移验证请用老库快照**：
   `_product\backup\galbox-real-db-before-migration-20260911-230250.db`（26 列，干净）。
4. **四条分支都基于同一个提交 `e320726`**，各自只动不同目录，理论上可干净合并——但 `Galbox.sln` 与 `App.xaml.cs` 有被多方修改的可能，合并时优先人工确认这两个文件。
